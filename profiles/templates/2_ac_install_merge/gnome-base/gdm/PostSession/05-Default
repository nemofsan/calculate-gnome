# Calculate name=Default append=replace chmod=0755
#!/bin/sh
# PostSession - running the script from the root user when the session ends

#?pkg(sys-apps/calculate-utils[desktop])!=#
/usr/share/calculate/xdm/xdm --logout
#pkg#
